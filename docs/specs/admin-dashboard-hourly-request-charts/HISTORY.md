# Admin 仪表盘请求趋势图表演进历史（#h2698）

- 2026-07-24: 两张自然日较昨日差值图被积分柱图与积分面积图替换。积分视图同时展示本地
  估算和上游实扣；前者来自请求 rollup，后者来自 quota 样本差分。未采样上游桶保持未知，
  避免将缺失数据误报为零。
- 2026-07-07: 面积图调整为最近 6 小时、5 分钟粒度，绝对柱图调整为滚动 25 个小时槽，
  并补充当前未满小时和缺口留空行为。
- 请求 rollup 从“异步写入即视为完整”收紧为可验证派生数据：保留期内以请求日志审计和修复，过期后以日级
  seal 维持可验证、可恢复的日统计。修复栅栏隔离 coalescer 中迟到的增量，热窗口优先且只将当前待审片
  显式留空，并公开完整性状态。封存日回审按 5 分钟片与热窗交错，取消的源修改会失效版本；GC 只对可见
  dashboard 源日要求 seal，避免抑制 retry shadow 无限制延长原始日志保留。

## Legacy Identity

- Legacy compatibility identity: `#h2698`.
