# Linux.do Credit 额度充值演进历史（#5vxmz）

> 这里记录会影响 Agent 理解“为什么一步步变成现在这样”的关键演进；单次任务流水账不放这里，规范正文仍以 `./SPEC.md` 为准。

## Decision Trace

- 2026-05-26: 新增本 topic spec，锁定官方 LDC 创建订单、月度额度按服务器本地自然月展开、充值权益只叠加账户月额度。
- 2026-05-27: 增加管理端系统设置开关。充值总开关负责展示与创建订单 gate；非管理员开放开关作为调试保护，允许管理员先自测充值链路再开放给普通用户。
- 2026-05-27: 默认价格调整为 `50 LDC = 1000 积分 / 自然月`，充值权益改为每
  `1000` 积分派生 `+20` 小时、`+100` 日、`+1000` 月额度。
- 2026-05-28: 线上商户私钥验证发现 Linux.do Credit 配置值为 48-byte 最小 Ed25519
  PKCS#8 v1 DER；签名实现补充该格式解析，避免误判为 `ring` 不接受的 PKCS#8 私钥。
- 2026-05-28: 线上创建订单确认上游会先生成 LDC 记录再返回支付页跳转；后端改为不跟随创建订单响应的重定向，而是把 `Location` 交给浏览器处理。
- 2026-05-28: 增加管理端充值记录和退款能力。退款按 Linux.do Credit `POST /epay/api.php`
  全额退回接口实现，并通过全局管理端 TOTP 二次确认保护真实资金链路；`DEV_OPEN_ADMIN`
  继续只能用于只读/本地调试，不能开启充值或退款。
- 2026-05-31: 线上验证发现未绑定管理端 TOTP 时后端已正确返回 `403 admin TOTP is not bound`，但充值记录页仍展示验证码输入并吞掉退款失败反馈；管理端 UI 改为先读取 TOTP 状态，未绑定时引导到系统设置，退款失败时在确认弹窗内显示错误。
- 2026-06-30: 残月充值开始使用服务端 quote 锁价与逐字段校验，当前月按剩余天数做线性 clamp / 折价，跨月未支付订单转 `expired`；订单记录与管理端列表开始显式展示最终成交值和月底折抵标记。
- 2026-07-12: 恢复 `/console` 概览右侧完整充值卡，复用现有充值状态；独立 billing 页继续负责完整权益、排期与订单明细。
- 2026-07-13: 充值订单生命周期改为本地 `10 分钟关闭支付入口 + 24 小时取消订单`。`expired` 改为本地关闭语义，新增 `cancelled`；成功回调若超过 24 小时或跨出 `quote_month_start` 则不再落成 `expired`，而是进入系统自动退款补偿路径。
- 2026-07-14: 管理端用户详情额度月历修正“已用额度”列，不再把当前月 `monthlyCreditsUsed` 复制到前后月份占位行；当前接口只提供本月汇总，因此仅当前月显示真实已用额度。
- 2026-07-26: 充值订单影响预览改用“本单前充值月额度 / 本单增加 / 本单后充值月额度”命名，英文表头收短为 `Before order / Added by order / After order`；补充桌面表头与移动抽屉帮助说明，并统一桌面数字列对齐。语义仍只描述充值权益，不改变 quote、订单或额度计算。

## Key Reasons / Replacements

- 新用户零基线额度和管理员手动加额无法满足用户自助购买；需要支付订单、权益和 quota 解析形成稳定闭环。
- Linux.do Credit 官方 LDC 创建订单要求 Ed25519 签名；异步通知文档未提供平台公钥，因此先按公共通知签名规则与 `Client Secret` 校验。
- 小时/日额度继续从有效充值权益动态派生，因此比例调整会影响仍有效的历史权益读取结果；订单价格只影响新订单。
- 商户私钥格式不应强绑定到单一 Rust crypto crate 接受的 PKCS#8 变体；服务应先规范化可识别的 Ed25519 seed，再执行签名。
- Linux.do Credit 支付页需要用户侧浏览器上下文；服务端 HTTP 客户端只能负责创建订单和提取跳转地址，不应代替用户访问支付页。
- 退款属于高风险管理员动作，使用全局 TOTP 而非单用户绑定，是为了适配当前管理端混合认证模型；恢复码和单管理员策略后续再独立设计。

## References

- `./SPEC.md`
- `./IMPLEMENTATION.md`

## Legacy Identity

- Legacy compatibility identity: `#5vxmz`.
