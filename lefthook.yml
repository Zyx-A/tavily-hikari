pre-commit:
  parallel: false
  commands:
    format-markdown:
      files: git diff --cached --name-only --diff-filter=ACMR -- '*.md'
      run: bunx --bun dprint fmt {files}
      stage_fixed: true
    fmt:
      glob: "**/*.rs"
      run: cargo fmt
      stage_fixed: true
    clippy:
      glob: "**/*.rs"
      run: cargo clippy --locked -j 2 -- -D warnings

commit-msg:
  commands:
    commitlint:
      run: bunx --bun commitlint --edit {1}
